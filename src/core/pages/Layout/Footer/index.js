import React, { Component } from 'react';
import { Layout } from 'antd';
const { Footer } = Layout;

export default class FooterTodoApp extends Component {
    render() {
        return (
            <Footer>Footer</Footer>
        );
    }
}